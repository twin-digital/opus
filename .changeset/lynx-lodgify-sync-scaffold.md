---
'@twin-digital/lynx-lodgify-sync': minor
---

Add the `lynx-lodgify-sync` app: a scheduled Lambda scaffold (logs on each run) and its self-contained AWS CDK stack (NodejsFunction + hourly EventBridge schedule).
