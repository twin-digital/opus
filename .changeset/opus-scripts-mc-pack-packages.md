---
'@twin-digital/opus-scripts': minor
---

Add `mc-pack-packages`, which detects behavior-pack packages (a committed `pack/manifest.json`) released at HEAD from git tags and emits a JSON matrix for the publish workflow's `.mcpack` release-asset job.
