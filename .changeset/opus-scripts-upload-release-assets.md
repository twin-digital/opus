---
'@twin-digital/opus-scripts': minor
---

Add `upload-release-assets`, which uploads a released package's `.release-assets/` artifacts (plus a generated `SHA256SUMS`) to its GitHub release — the per-package step of the publish workflow's release-assets matrix.
