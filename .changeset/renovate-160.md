---
'@twin-digital/eslint-config': minor
---

chore(deps): update eslint monorepo to v10. ESLint v10 drops the legacy eslintrc system and adds three rules to `eslint:recommended` (`no-unassigned-vars`, `no-useless-assignment`, `preserve-caught-error`), so consumers of this config will see those enforced.
