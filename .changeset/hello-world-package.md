---
'@twin-digital/hello-world': minor
---

New behavior pack that greets players when they join — a minimal standalone example (no shared-lib dependency) of the pack authoring/dev/publish workflow. Each release attaches an installable `.mcpack` to the package's GitHub release.
