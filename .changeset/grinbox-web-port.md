---
'@grinbox/web': minor
---

Port grinbox's browser application into the workspace. It types its API client from the daemon's own routes, bundles its fonts so it renders without egress, and no longer offers a mail-backend deep link.
