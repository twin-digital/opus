---
---

docs: update CICD.md, renovate-integration.md, and github-app-token-minting.md to the current credential model — environment-scoped secrets, role ARNs/App ids as repo variables, no repo-level secrets (COCOBOT and CHANGESETS_GITHUB_TOKEN removed), and deploy jobs checking out with the default github.token.
