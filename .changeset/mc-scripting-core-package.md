---
'@twin-digital/mc-scripting-core': minor
---

New package: shared helpers for Minecraft Bedrock behavior packs — entity tags and an invulnerability guard (tag + resistance + heal-on-hurt) that registers itself lazily on first use. `setInvulnerable(entity, { enabled, showParticles })` takes an options object.
