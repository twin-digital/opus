---
'@twin-digital/mc-scripting-core': minor
---

`setInvulnerable(entity, { enabled, showParticles })` makes an entity invulnerable — an invulnerability tag plus hidden Resistance — or clears both with `enabled: false`.
