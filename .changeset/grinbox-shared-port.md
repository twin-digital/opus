---
'@grinbox/shared': minor
---

Port grinbox's shared contracts into the workspace. The package declares the value vocabulary both tiers speak — the closed enums, the operator configuration schemas, the contract derivation, the resource and limit registries, and the match and template grammars.
