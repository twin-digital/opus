---
---

docs(farwatch): add a chronicler authoring/extending guide and route to it.

Elevate the stale `chronicler/pipelines/README.md` (which claimed the executor/UI were unbuilt and described the retired per-template override model) to `chronicler/README.md`, rewritten as orientation + recipes for the prompt builder, snippets/axes, structured output, pipelines, the inspector, and backends — routing to the code and the glossary for detail. Add a pointer from `farwatch/CLAUDE.md`. Docs only; no package behavior changes.
