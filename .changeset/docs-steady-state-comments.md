---
---

docs(devcontainer): describe the `credential-shelf` and `workspace` images at steady state — drop the transition narrative (folding base+default, the features migration, "consolidated", pinned-versions-are-starting-points) from comments and READMEs.
