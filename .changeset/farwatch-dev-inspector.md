---
'@thrashplay/farwatch': patch
---

feat(farwatch): add a dev-only web inspector (`pnpm --filter @thrashplay/farwatch serve`) that runs the seed → resolve → chronicle pipeline and shows the chronicle prose beside the fully-exposed guts (pinned `AdventureResult`, the exact prompt, and the raw completion). Debug superset of the eventual player view — hides nothing.
