---
---

The Minecraft dev server deploys packs via `docker compose watch`: a pack's built `dist/` syncs into the container followed by `/reload`, and activation-list changes sync into the world followed by a restart. The per-pack watch rules and the activation list are generated by `generate-dev-config.mjs` — packs are discovered, not hand-listed. No package code changes.
