---
'@thrashplay/music': patch
---

Studio watcher: a clip whose library entry has no source files (from before the library, or a region made by hand) gets them from the items under its region, so the producer import has its stems.
