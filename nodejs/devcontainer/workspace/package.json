{
  "private": true,
  "name": "@twin-digital/workspace",
  "version": "0.1.0",
  "description": "Dev container image — security hardening (no sudo, VS Code channel scrub, credential shims) plus common tooling.",
  "type": "module",
  "scripts": {
    "artifact": "artifact"
  },
  "devDependencies": {
    "@twin-digital/opus-scripts": "workspace:*"
  },
  "engines": {
    "node": "24.x",
    "pnpm": "10.x"
  }
}
