org: twindigital
service: discord-bot

stages:
  default:
    params:
      profile: 775111556400-AdministratorAccess
      region: us-east-2
  prod:
    params:
      profile: 775111556400-AdministratorAccess
      region: us-east-2
  dev:
    params:
      profile: 775111556400-AdministratorAccess
      region: us-east-2

provider:
  name: aws
  runtime: nodejs22.x
  stage: ${opt:stage, 'dev'}
  region: ${param:region}
  profile: ${param:profile}

functions:
  poc:
    handler: src/functions/poc.handler
