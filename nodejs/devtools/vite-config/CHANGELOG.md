# @twin-digital/vite-config

## 0.2.0

### Minor Changes

- da1e483: New package: the shared vite base config plus `defineAppConfig(import.meta.url)`, which composes it with a package's `vite.config.d/*.js` overrides.
