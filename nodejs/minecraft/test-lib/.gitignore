# The generated classes are rebuilt by tools/generate.mjs (prebuild/prepare); the manifests beside
# them are committed, so a version bump reads as a diff of what moved.
src/generated/fakes/
src/generated/index.ts
