version: '1'

# The world this adventure is developed against. The spawn matches the adventure's STAGE constant,
# so a run starts beside the actor gallery.
level: rpg-core-example-dev
seed: 424242
spawn: [465, 70, -64]

eula: true

# Only this adventure: its actors are content of its own packs, so there is no second pack to install.
defaultProfile: example
profiles:
  example:
    packs: ['@twin-digital/rpg-core-example']
