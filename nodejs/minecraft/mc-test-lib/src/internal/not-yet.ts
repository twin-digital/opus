/** Placeholder body for members whose implementation lands in a later build wave. */
export const notYetImplemented = (): never => {
  throw new Error('@twin-digital/minecraft-test-lib: not yet implemented (build in progress)')
}
