{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", "helpers:pinGitHubActionDigests", "group:allNonMajor"],
  "schedule": ["before 6am on monday"],
  "minimumReleaseAge": "5 days",
  "internalChecksFilter": "strict",
  "prConcurrentLimit": 10,
  "prHourlyLimit": 0,
  "labels": ["dependencies"],
  "gitIgnoredAuthors": ["290907000+twin-digital-agent[bot]@users.noreply.github.com"],
  "vulnerabilityAlerts": {
    "automerge": true
  },
  "packageRules": [
    {
      "description": "Packages with install/build scripts (pnpm `onlyBuiltDependencies` in pnpm-workspace.yaml) run arbitrary code on install. Pull each out of the `group:allNonMajor` batch into its own PR with a distinct title/label, and never auto-merge — so the script-running bump is reviewed in isolation. Keep this list in sync with `onlyBuiltDependencies`.",
      "matchPackageNames": ["esbuild", "hnswlib-node", "serverless"],
      "groupName": null,
      "automerge": false,
      "addLabels": ["build-scripts"],
      "commitMessageTopic": "build-script dependency {{depName}}"
    },
    {
      "description": "Packages with a pnpm patch (`patchedDependencies` in pnpm-workspace.yaml) carry a version-pinned patch; a bump leaves the patch unapplied and breaks the lockfile relock (`pnpm install` fails). Pull each out of the `group:allNonMajor` batch into its own PR with a distinct title/label, and never auto-merge — so a human re-rolls or drops the patch under review. `matchPackageNames` is kept in sync with `patchedDependencies` by repo-kit; do not edit it by hand.",
      "matchPackageNames": ["ink", "@mishieck/ink-titled-box"],
      "groupName": null,
      "automerge": false,
      "addLabels": ["patched-deps"],
      "commitMessageTopic": "patched dependency {{depName}}"
    },
    {
      "description": "repo-kit templates all .nvmrc files; a Renovate bump is reverted by `pnpm sync` and fails the merge-checks gate",
      "matchManagers": ["nvm"],
      "enabled": false
    },
    {
      "description": "engines.node / engines.pnpm are written by repo-kit (.repo-kit.yml); updating them fails the sync gate",
      "matchDepTypes": ["engines"],
      "enabled": false
    },
    {
      "description": "tsdown's version is owned by repo-kit (.repo-kit.yml), not Renovate",
      "matchPackageNames": ["tsdown"],
      "enabled": false
    }
  ]
}
